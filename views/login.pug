extends layouts/login-layout.pug

block content
  .loginContainer
    h1 Login
    form(method="POST")

      p.errorMessage #{errorMessage }
      
      //- input(type="text", name="logUsername", placeholder="Username or email.", required="")
      //- input(type="password", name="logPassword", placeholder="Password.", required="")
      
      input(type="text", name="logUsername", placeholder="Username or email.", value="test" required="")
      input(type="password", name="logPassword", placeholder="Password.", value="tester" required="")
      input(type="submit", placeholder="Login.")

    a(href="/register") Neea an account? Register here